dist: xenial
language: generic
python:
  - 3.6
  - 3.7
node_js:
  - 8
  - 10
cache:
  - pip
  - npm
install:
  - pip install -r requirements.txt
  - cd third_party/node && npm install
script:
  - make lint
  - make test
